# URL of the nurse-scheduling optimization backend the web frontend talks to.
# Resolved at build time (NEXT_PUBLIC_ vars are inlined into the static export),
# so set it before running `next build`. Defaults to http://localhost:8000 when
# unset. No trailing slash required.
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000
